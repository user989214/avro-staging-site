{%- comment -%}
  ============================================================================
  AVRO — SUBSCRIBE PAGE  (Shopify Online Store 2.0 section)
  ----------------------------------------------------------------------------
  HOW TO USE
  1. In your Shopify theme, create:  sections/avro-subscribe.liquid
     Paste the ENTIRE contents of this file into it (including the
     schema block at the bottom). Save.
  2. Images render from hardcoded live-site URLs by default — no asset upload
     needed. Pick an image in the editor to override any of them.
  3. Add the section to a page:
     - Create a "Subscribe" page and assign a template that renders this
       section, OR add "AVRO — Subscribe" via the theme editor.
  4. Everything labelled in the theme editor (headings, body copy, button
     labels/links, and images) is editable. Repeating items (benefits, steps,
     formula cards) are editable BLOCKS — add / remove / reorder them.

  NOTE ON IMAGES: each image / icon setting falls back to a hardcoded live-site
  URL when nothing is chosen in the editor, so the page looks correct
  immediately after import. Pick an image in the editor to override.
  ============================================================================
{%- endcomment -%}

{% style %}
  /* ── AVRO design tokens (scoped to this section) ───────────────────────── */
  #avro-sub-{{ section.id }} {
    --base: #FFFFFF;
    --base-light: #F2F2F2;
    --base-deep: #EDEDED;
    --bone: #FFFFFF;
    --charcoal: #151515;
    --ink: #1C1B14;
    --warm-gray: #6B6857;
    --divider: #E2E2E2;
    --avro-blue: #94C6D4;
    --avro-blue-deep: #5AAEBC;
    font-family: "DM Sans", system-ui, sans-serif;
    background-color: var(--base);
    color: var(--ink);
    display: block;
  }
  #avro-sub-{{ section.id }} * { box-sizing: border-box; }
  #avro-sub-{{ section.id }} h1,
  #avro-sub-{{ section.id }} h2,
  #avro-sub-{{ section.id }} h3 { margin: 0; }
  #avro-sub-{{ section.id }} p { margin: 0; }
  #avro-sub-{{ section.id }} img { max-width: 100%; }

  #avro-sub-{{ section.id }} .sub-wrap { max-width: 1250px; margin: 0 auto; }

  /* Buttons */
  #avro-sub-{{ section.id }} .sub-btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    font-weight: 700; border-radius: 999px; text-decoration: none; cursor: pointer;
    min-height: 48px; padding: 0 28px; font-size: 16px;
    transition: background-color .2s ease, color .2s ease, border-color .2s ease;
  }
  #avro-sub-{{ section.id }} .sub-btn-dark { background: var(--charcoal); color: var(--bone); border: 2px solid var(--charcoal); }
  #avro-sub-{{ section.id }} .sub-btn-dark:hover { background: transparent; color: var(--charcoal); }
  #avro-sub-{{ section.id }} .sub-btn-outline { background: transparent; color: var(--charcoal); border: 2px solid var(--charcoal); }
  #avro-sub-{{ section.id }} .sub-btn-outline:hover { background: var(--charcoal); color: var(--bone); }

  /* ── HERO ─────────────────────────────────────────────────────────────── */
  #avro-sub-{{ section.id }} .sub-hero {
    position: relative; width: calc(100% - 32px); margin: 16px auto 0;
    border-radius: 20px; overflow: hidden; background: var(--base-light);
  }
  #avro-sub-{{ section.id }} .sub-hero-inner {
    position: relative; z-index: 1;
    padding: clamp(40px,7vw,96px) clamp(28px,5vw,72px);
    display: flex; flex-direction: column; align-items: flex-start;
  }
  #avro-sub-{{ section.id }} .sub-hero-bg { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; opacity: 0.9; }
  #avro-sub-{{ section.id }} .sub-hero.has-bg .sub-hero-inner { background: linear-gradient(90deg, rgba(255,255,255,0.92) 0%, rgba(255,255,255,0.75) 55%, rgba(255,255,255,0.25) 100%); }
  #avro-sub-{{ section.id }} .sub-hero h1 {
    font-weight: 700; font-size: clamp(40px,6vw,72px); line-height: 1.0;
    letter-spacing: -0.03em; color: var(--ink); margin-bottom: 20px; max-width: 640px;
  }
  #avro-sub-{{ section.id }} .sub-hero p.lede {
    font-weight: 400; font-size: clamp(16px,1.4vw,19px); line-height: 1.55;
    color: var(--ink); max-width: 560px; margin-bottom: 28px;
  }
  #avro-sub-{{ section.id }} .sub-hero-btns { display: flex; flex-wrap: wrap; gap: 12px; }

  /* ── SHARED SECTION HEADINGS ──────────────────────────────────────────── */
  #avro-sub-{{ section.id }} .sub-sec { padding: clamp(40px,6vw,80px) clamp(20px,5vw,64px); }
  #avro-sub-{{ section.id }} .sub-sec > .sub-wrap > h2 {
    font-weight: 700; font-size: clamp(28px,3.6vw,52px); line-height: 1;
    letter-spacing: -0.02em; color: var(--ink); margin-bottom: 8px;
  }
  #avro-sub-{{ section.id }} .sub-sec > .sub-wrap > p.intro {
    font-weight: 500; font-size: clamp(15px,1.3vw,19px); line-height: 1.45;
    color: var(--warm-gray); max-width: 640px; margin-bottom: 28px;
  }

  /* ── WHY SUBSCRIBE / HOW IT WORKS (shared card grid) ──────────────────── */
  #avro-sub-{{ section.id }} .why-sec { background: var(--base-light); }
  #avro-sub-{{ section.id }} .why-sec .why-shell { background: var(--bone); border-radius: 24px; padding: clamp(24px,4vw,56px); }
  #avro-sub-{{ section.id }} .info-grid { display: grid; grid-template-columns: 1fr; gap: clamp(24px,3vw,40px); margin-top: 8px; }
  @media (min-width: 768px) { #avro-sub-{{ section.id }} .info-grid { grid-template-columns: repeat(3,1fr); } }
  #avro-sub-{{ section.id }} .info-card { display: flex; flex-direction: column; gap: 12px; }
  #avro-sub-{{ section.id }} .info-icon { width: 48px; height: 48px; display: flex; align-items: center; justify-content: center; }
  #avro-sub-{{ section.id }} .info-icon span {
    display: inline-block; width: 44px; height: 44px;
    -webkit-mask-repeat: no-repeat; mask-repeat: no-repeat;
    -webkit-mask-position: center; mask-position: center;
    -webkit-mask-size: contain; mask-size: contain;
  }
  #avro-sub-{{ section.id }} .info-card h3 { font-weight: 700; font-size: clamp(17px,1.5vw,21px); line-height: 1.2; color: var(--ink); }
  #avro-sub-{{ section.id }} .info-card p { font-weight: 400; font-size: clamp(14px,1.1vw,16px); line-height: 1.5; color: var(--warm-gray); }

  #avro-sub-{{ section.id }} .step-grid { display: grid; grid-template-columns: 1fr; gap: 12px; }
  @media (min-width: 768px) { #avro-sub-{{ section.id }} .step-grid { grid-template-columns: repeat(3,1fr); } }
  #avro-sub-{{ section.id }} .step-card { background: var(--base-light); border-radius: 16px; padding: clamp(20px,2.5vw,32px); display: flex; flex-direction: column; gap: 12px; }
  #avro-sub-{{ section.id }} .step-icon { width: 48px; height: 48px; display: flex; align-items: center; justify-content: center; }
  #avro-sub-{{ section.id }} .step-icon span {
    display: inline-block; width: 44px; height: 44px;
    -webkit-mask-repeat: no-repeat; mask-repeat: no-repeat;
    -webkit-mask-position: center; mask-position: center;
    -webkit-mask-size: contain; mask-size: contain;
  }
  #avro-sub-{{ section.id }} .step-card h3 { font-weight: 700; font-size: clamp(16px,1.4vw,20px); line-height: 1.2; color: var(--ink); }
  #avro-sub-{{ section.id }} .step-card p { font-weight: 400; font-size: clamp(14px,1.1vw,16px); line-height: 1.5; color: var(--warm-gray); }

  /* ── CHOOSE YOUR FORMULA ──────────────────────────────────────────────── */
  #avro-sub-{{ section.id }} .formula-sec { background: var(--base-light); }
  #avro-sub-{{ section.id }} .formula-row { display: flex; flex-direction: column; gap: 32px; }
  #avro-sub-{{ section.id }} .formula-card { display: flex; flex-direction: column; gap: 16px; }
  #avro-sub-{{ section.id }} .formula-thumb { border-radius: 20px; aspect-ratio: 1/1; display: flex; align-items: center; justify-content: center; overflow: hidden; background: #E5E5E5; }
  #avro-sub-{{ section.id }} .formula-thumb img { width: 100%; height: 100%; object-fit: cover; }
  #avro-sub-{{ section.id }} .formula-card h3 { font-weight: 700; font-size: clamp(22px,2vw,32px); color: var(--ink); }
  #avro-sub-{{ section.id }} .formula-card p.support { font-weight: 400; font-size: clamp(16px,1.3vw,20px); line-height: 1.45; color: var(--warm-gray); min-height: 2.9em; }
  #avro-sub-{{ section.id }} .formula-meta { border-top: 1px solid rgba(0,0,0,0.08); padding-top: 16px; display: flex; flex-direction: column; gap: 12px; }
  #avro-sub-{{ section.id }} .formula-meta .lbl { font-weight: 700; font-size: clamp(14px,1.1vw,17px); color: var(--ink); }
  #avro-sub-{{ section.id }} .formula-meta p { font-weight: 400; font-size: clamp(14px,1.05vw,16px); line-height: 1.5; color: var(--warm-gray); margin: 4px 0 0; }
  #avro-sub-{{ section.id }} .formula-meta .caf { font-weight: 700; font-size: clamp(14px,1.05vw,16px); color: var(--ink); }
  #avro-sub-{{ section.id }} .formula-card .sub-btn { width: 100%; margin-top: auto; }
  @media (min-width: 640px) {
    #avro-sub-{{ section.id }} .formula-row { flex-direction: row; gap: 24px; }
    #avro-sub-{{ section.id }} .formula-card { flex: 1 1 0; }
  }

  /* ── BOTTOM CTA BAND ──────────────────────────────────────────────────── */
  #avro-sub-{{ section.id }} .cta-band { background: var(--avro-blue); padding: clamp(48px,7vw,96px) clamp(20px,5vw,64px); }
  #avro-sub-{{ section.id }} .cta-inner { max-width: 1250px; margin: 0 auto; display: flex; flex-direction: column; gap: 28px; }
  #avro-sub-{{ section.id }} .cta-eyebrow { font-weight: 700; font-size: 13px; letter-spacing: 0.14em; text-transform: uppercase; color: rgba(21,21,21,0.7); }
  #avro-sub-{{ section.id }} .cta-band h2 { font-weight: 700; font-size: clamp(30px,4vw,56px); line-height: 1.02; letter-spacing: -0.02em; color: var(--charcoal); margin-top: 8px; }
  #avro-sub-{{ section.id }} .cta-band p { font-weight: 500; font-size: clamp(15px,1.3vw,18px); line-height: 1.5; color: rgba(21,21,21,0.72); max-width: 560px; margin-top: 12px; }
  #avro-sub-{{ section.id }} .cta-btns { display: flex; flex-direction: column; gap: 8px; max-width: 400px; }
  #avro-sub-{{ section.id }} .cta-btns .sub-btn { width: 100%; background: var(--charcoal); color: var(--bone); border: 2px solid var(--charcoal); }
  #avro-sub-{{ section.id }} .cta-btns .sub-btn:hover { background: transparent; color: var(--charcoal); }
  @media (min-width: 860px) {
    #avro-sub-{{ section.id }} .cta-inner { flex-direction: row; align-items: center; justify-content: space-between; gap: 40px; }
    #avro-sub-{{ section.id }} .cta-left { flex: 1 1 0; max-width: 620px; }
    #avro-sub-{{ section.id }} .cta-btns { flex: 0 0 340px; }
  }
{% endstyle %}

{%- capture arrow_svg -%}<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>{%- endcapture -%}

<div id="avro-sub-{{ section.id }}">

  {%- comment -%} ============ HERO ============ {%- endcomment -%}
  {%- assign has_hero_bg = false -%}
  {%- if section.settings.hero_image != blank or section.settings.hero_image_fallback != blank -%}{%- assign has_hero_bg = true -%}{%- endif -%}
  <section class="sub-hero{% if has_hero_bg %} has-bg{% endif %}">
    {%- if section.settings.hero_image != blank -%}
      <img class="sub-hero-bg" src="{{ section.settings.hero_image | image_url: width: 2400 }}" alt="" loading="eager">
    {%- elsif section.settings.hero_image_fallback != blank -%}
      <img class="sub-hero-bg" src="{{ section.settings.hero_image_fallback }}" alt="" loading="eager">
    {%- endif -%}
    <div class="sub-hero-inner">
      <h1>{{ section.settings.hero_heading | default: 'Subscribe + Save.' }}</h1>
      <p class="lede">{{ section.settings.hero_body | default: 'Subscribe & save 15% every order of 2 or more tubes. Select your own delivery schedule, get free shipping on orders of 2 or more tubes, and skip, change or cancel anytime.' }}</p>
      <div class="sub-hero-btns">
        <a href="{{ section.settings.hero_btn1_link | default: '/collections/all' }}" class="sub-btn sub-btn-dark">{{ section.settings.hero_btn1_label | default: 'Start Your Subscription' }}</a>
        <a href="{{ section.settings.hero_btn2_link | default: '/collections/all' }}" class="sub-btn sub-btn-outline">{{ section.settings.hero_btn2_label | default: 'Shop All Products' }}</a>
      </div>
    </div>
  </section>

  {%- comment -%} ============ WHY SUBSCRIBE ============ {%- endcomment -%}
  {%- assign benefits = section.blocks | where: "type", "benefit" -%}
  {%- if benefits.size > 0 -%}
  <section class="sub-sec why-sec">
    <div class="sub-wrap">
      <div class="why-shell">
        <h2 style="margin-bottom:8px;">{{ section.settings.why_heading | default: 'Why Subscribe.' }}</h2>
        <div class="info-grid">
          {%- for b in benefits -%}
          <article class="info-card" {{ b.shopify_attributes }}>
            <span class="info-icon">
              {%- if b.settings.icon != blank -%}
                {%- assign icon_url = b.settings.icon | image_url: width: 120 -%}
              {%- else -%}
                {%- assign icon_url = b.settings.icon_fallback | default: 'https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/consistency-and-quality.png' -%}
              {%- endif -%}
              <span style="background-color: {{ b.settings.icon_color | default: '#E8A23D' }}; -webkit-mask-image: url({{ icon_url }}); mask-image: url({{ icon_url }});"></span>
            </span>
            <h3>{{ b.settings.title }}</h3>
            <p>{{ b.settings.copy }}</p>
          </article>
          {%- endfor -%}
        </div>
      </div>
    </div>
  </section>
  {%- endif -%}

  {%- comment -%} ============ HOW IT WORKS ============ {%- endcomment -%}
  {%- assign steps = section.blocks | where: "type", "step" -%}
  {%- if steps.size > 0 -%}
  <section class="sub-sec">
    <div class="sub-wrap">
      <h2 style="margin-bottom:24px;">{{ section.settings.how_heading | default: 'How It Works.' }}</h2>
      <div class="step-grid">
        {%- for b in steps -%}
        <article class="step-card" {{ b.shopify_attributes }}>
          <span class="step-icon">
            {%- if b.settings.icon != blank -%}
              {%- assign icon_url = b.settings.icon | image_url: width: 120 -%}
            {%- else -%}
              {%- assign icon_url = b.settings.icon_fallback | default: 'https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/calm-first-foundation.png' -%}
            {%- endif -%}
            <span style="background-color: {{ b.settings.icon_color | default: '#5AAEBC' }}; -webkit-mask-image: url({{ icon_url }}); mask-image: url({{ icon_url }});"></span>
          </span>
          <h3>{{ b.settings.title }}</h3>
          <p>{{ b.settings.copy }}</p>
        </article>
        {%- endfor -%}
      </div>
    </div>
  </section>
  {%- endif -%}

  {%- comment -%} ============ CHOOSE YOUR FORMULA ============ {%- endcomment -%}
  {%- assign formulas = section.blocks | where: "type", "formula" -%}
  {%- if formulas.size > 0 -%}
  <section class="sub-sec formula-sec">
    <div class="sub-wrap">
      <h2>{{ section.settings.formula_heading | default: 'Choose Your Formula.' }}</h2>
      <p class="intro">{{ section.settings.formula_intro | default: 'Every AVRO formula starts with the same calm-first base, then supports the moment in a different way.' }}</p>
      <div class="formula-row">
        {%- for b in formulas -%}
        <article class="formula-card" {{ b.shopify_attributes }}>
          <div class="formula-thumb">
            {%- if b.settings.image != blank -%}
              <img src="{{ b.settings.image | image_url: width: 800 }}" alt="{{ b.settings.name | escape }}">
            {%- else -%}
              <img src="{{ b.settings.image_fallback | default: 'https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/calm-blueberry-acai.png' }}" alt="{{ b.settings.name | escape }}">
            {%- endif -%}
          </div>
          <h3>{{ b.settings.name }}</h3>
          <p class="support">{{ b.settings.support }}</p>
          <div class="formula-meta">
            <div>
              <span class="lbl">Best For:</span>
              <p>{{ b.settings.best_for }}</p>
            </div>
            <span class="caf">{{ b.settings.caffeine }}</span>
          </div>
          <a href="{{ b.settings.link | default: '/collections/all' }}" class="sub-btn sub-btn-outline">{{ b.settings.button_label | default: 'Shop' }}</a>
        </article>
        {%- endfor -%}
      </div>
    </div>
  </section>
  {%- endif -%}

  {%- comment -%} ============ BOTTOM CTA BAND ============ {%- endcomment -%}
  <section class="cta-band">
    <div class="cta-inner">
      <div class="cta-left">
        <span class="cta-eyebrow">{{ section.settings.cta_eyebrow | default: 'Subscribe & Save 15%' }}</span>
        <h2>{{ section.settings.cta_heading | default: 'Calm-First Support, On Your Schedule.' }}</h2>
        <p>{{ section.settings.cta_body | default: 'Subscribe & save 15% every order of 2 or more tubes, with free shipping on orders of 2 or more tubes. Skip, change or cancel anytime.' }}</p>
      </div>
      <div class="cta-btns">
        <a href="{{ section.settings.cta_btn1_link | default: '/collections/calm' }}" class="sub-btn">{{ section.settings.cta_btn1_label | default: 'Shop Calm' }}</a>
        <a href="{{ section.settings.cta_btn2_link | default: '/collections/focus' }}" class="sub-btn">{{ section.settings.cta_btn2_label | default: 'Shop Focus' }}</a>
        <a href="{{ section.settings.cta_btn3_link | default: '/collections/energy' }}" class="sub-btn">{{ section.settings.cta_btn3_label | default: 'Shop Energy' }}</a>
      </div>
    </div>
  </section>

</div>

{% schema %}
{
  "name": "AVRO — Subscribe",
  "tag": "section",
  "class": "avro-subscribe-section",
  "settings": [
    { "type": "header", "content": "Hero" },
    { "type": "text", "id": "hero_heading", "label": "Heading", "default": "Subscribe + Save." },
    { "type": "textarea", "id": "hero_body", "label": "Body", "default": "Subscribe & save 15% every order of 2 or more tubes. Select your own delivery schedule, get free shipping on orders of 2 or more tubes, and skip, change or cancel anytime." },
    { "type": "image_picker", "id": "hero_image", "label": "Background image (optional)" },
    { "type": "text", "id": "hero_image_fallback", "label": "Background image URL (optional)", "info": "Leave blank for a plain light background." },
    { "type": "text", "id": "hero_btn1_label", "label": "Button 1 label", "default": "Start Your Subscription" },
    { "type": "url", "id": "hero_btn1_link", "label": "Button 1 link" },
    { "type": "text", "id": "hero_btn2_label", "label": "Button 2 label", "default": "Shop All Products" },
    { "type": "url", "id": "hero_btn2_link", "label": "Button 2 link" },

    { "type": "header", "content": "Why Subscribe" },
    { "type": "text", "id": "why_heading", "label": "Heading", "default": "Why Subscribe." },

    { "type": "header", "content": "How It Works" },
    { "type": "text", "id": "how_heading", "label": "Heading", "default": "How It Works." },

    { "type": "header", "content": "Choose Your Formula" },
    { "type": "text", "id": "formula_heading", "label": "Heading", "default": "Choose Your Formula." },
    { "type": "richtext", "id": "formula_intro", "label": "Intro" },

    { "type": "header", "content": "Bottom CTA band" },
    { "type": "text", "id": "cta_eyebrow", "label": "Eyebrow", "default": "Subscribe & Save 15%" },
    { "type": "text", "id": "cta_heading", "label": "Heading", "default": "Calm-First Support, On Your Schedule." },
    { "type": "textarea", "id": "cta_body", "label": "Body", "default": "Subscribe & save 15% every order of 2 or more tubes, with free shipping on orders of 2 or more tubes. Skip, change or cancel anytime." },
    { "type": "text", "id": "cta_btn1_label", "label": "Button 1 label", "default": "Shop Calm" },
    { "type": "url", "id": "cta_btn1_link", "label": "Button 1 link" },
    { "type": "text", "id": "cta_btn2_label", "label": "Button 2 label", "default": "Shop Focus" },
    { "type": "url", "id": "cta_btn2_link", "label": "Button 2 link" },
    { "type": "text", "id": "cta_btn3_label", "label": "Button 3 label", "default": "Shop Energy" },
    { "type": "url", "id": "cta_btn3_link", "label": "Button 3 link" }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Why Subscribe item",
      "settings": [
        { "type": "image_picker", "id": "icon", "label": "Icon (PNG, masked to color)" },
        { "type": "text", "id": "icon_fallback", "label": "Icon URL", "default": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/consistency-and-quality.png" },
        { "type": "color", "id": "icon_color", "label": "Icon color", "default": "#E8A23D" },
        { "type": "text", "id": "title", "label": "Title", "default": "Save On Every Order" },
        { "type": "textarea", "id": "copy", "label": "Copy" }
      ]
    },
    {
      "type": "step",
      "name": "How It Works step",
      "settings": [
        { "type": "image_picker", "id": "icon", "label": "Icon (PNG, masked to color)" },
        { "type": "text", "id": "icon_fallback", "label": "Icon URL", "default": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/calm-first-foundation.png" },
        { "type": "color", "id": "icon_color", "label": "Icon color", "default": "#5AAEBC" },
        { "type": "text", "id": "title", "label": "Title", "default": "1. Choose Your Formula" },
        { "type": "textarea", "id": "copy", "label": "Copy" }
      ]
    },
    {
      "type": "formula",
      "name": "Formula card",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Product image" },
        { "type": "text", "id": "image_fallback", "label": "Image URL", "default": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/calm-blueberry-acai.png" },
        { "type": "text", "id": "name", "label": "Name", "default": "AVRO Calm" },
        { "type": "textarea", "id": "support", "label": "Support line" },
        { "type": "textarea", "id": "best_for", "label": "Best for" },
        { "type": "text", "id": "caffeine", "label": "Caffeine line", "default": "No Caffeine" },
        { "type": "text", "id": "button_label", "label": "Button label", "default": "Shop Calm" },
        { "type": "url", "id": "link", "label": "Button link" }
      ]
    }
  ],
  "presets": [
    {
      "name": "AVRO — Subscribe",
      "blocks": [
        { "type": "benefit", "settings": { "icon_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/consistency-and-quality.png", "icon_color": "#E8A23D", "title": "Save On Every Order", "copy": "Subscribers get 15% off every order — the best price on AVRO, automatically." } },
        { "type": "benefit", "settings": { "icon_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/control-under-pressure.png", "icon_color": "#E8A23D", "title": "Total Flexibility", "copy": "Select your own delivery schedule, skip a shipment, or change and cancel anytime. No commitments, no hassle." } },
        { "type": "benefit", "settings": { "icon_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/ingredient-disclosure.png", "icon_color": "#5AAEBC", "title": "Never Run Out", "copy": "Your formula arrives on a schedule that fits your routine, so calm-first support is always on hand." } },
        { "type": "step", "settings": { "icon_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/calm-first-foundation.png", "icon_color": "#5AAEBC", "title": "1. Choose Your Formula", "copy": "Select the AVRO formula that fits your needs — Calm, Focus, or Energy." } },
        { "type": "step", "settings": { "icon_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/step-pour.png", "icon_color": "#5AAEBC", "title": "2. Set Your Schedule", "copy": "Select your own delivery schedule — every 30, 45, or 60 days." } },
        { "type": "step", "settings": { "icon_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/icons/third-party-tested.png", "icon_color": "#5AAEBC", "title": "3. Save 15% Every Time", "copy": "Orders of 2 or more tubes ship automatically with 15% off and free shipping." } },
        { "type": "formula", "settings": { "image_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/calm-blueberry-acai.png", "name": "AVRO Calm", "support": "Calm support without stimulation.*", "best_for": "Golf, competition, travel, social situations, and pressure-filled moments.*", "caffeine": "No Caffeine", "button_label": "Shop Calm", "link": "/collections/calm" } },
        { "type": "formula", "settings": { "image_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/focus-pomegranate-raspberry.png", "name": "AVRO Focus", "support": "Clarity and focus support without caffeine.*", "best_for": "Deep work, studying, gaming, creative work, and longer attention blocks.*", "caffeine": "No Caffeine", "button_label": "Shop Focus", "link": "/collections/focus" } },
        { "type": "formula", "settings": { "image_fallback": "https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/energy-fuji-apple.png", "name": "AVRO Energy", "support": "Natural caffeine with a calm-first foundation.*", "best_for": "Early starts, long workdays, training, competition, and steady energy.*", "caffeine": "120mg Natural Caffeine", "button_label": "Shop Energy", "link": "/collections/energy" } }
      ]
    }
  ]
}
{% endschema %}
