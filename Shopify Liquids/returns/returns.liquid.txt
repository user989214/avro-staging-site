{%- comment -%}
  ============================================================================
  AVRO — RETURNS & REFUNDS  (Shopify Online Store 2.0 section)
  ----------------------------------------------------------------------------
  HOW TO USE
  1. Create sections/avro-returns.liquid and paste this entire file in.
  2. Add it to a page (e.g. a "Returns & Refunds" page) via the theme editor,
     or render it from a page template.
  3. The title, lede, "last updated" date, and every policy section
     (heading + body) are editable. Policy sections are repeatable BLOCKS —
     add / remove / reorder them in the theme editor.
  No images are required for this page.
  ============================================================================
{%- endcomment -%}

{% style %}
  #avro-legal-{{ section.id }} {
    --base: #FFFFFF; --bone: #FFFFFF; --charcoal: #151515; --ink: #1C1B14;
    --warm-gray: #6B6857; --divider: #E2E2E2;
    font-family: "DM Sans", system-ui, sans-serif;
    background-color: var(--base); color: var(--ink); display: block;
  }
  #avro-legal-{{ section.id }} * { box-sizing: border-box; }
  #avro-legal-{{ section.id }} .legal-hero { padding: clamp(48px,7vw,96px) clamp(20px,5vw,64px) clamp(8px,2vw,24px); text-align: center; }
  #avro-legal-{{ section.id }} .legal-hero h1 { font-weight: 800; font-size: clamp(34px,5vw,60px); line-height: 1.02; letter-spacing: -0.02em; color: var(--ink); margin: 0 auto; max-width: 760px; }
  #avro-legal-{{ section.id }} .legal-hero p { font-weight: 400; font-size: clamp(16px,1.4vw,19px); line-height: 1.55; color: var(--warm-gray); margin: 16px auto 0; max-width: 620px; }
  #avro-legal-{{ section.id }} .legal-body { padding: clamp(40px,5vw,72px) clamp(20px,5vw,64px); }
  #avro-legal-{{ section.id }} .legal-wrap { margin: 0 auto; width: 100%; max-width: 760px; }
  #avro-legal-{{ section.id }} .legal-updated { font-size: 13px; color: var(--warm-gray); margin: 0 0 32px; }
  #avro-legal-{{ section.id }} .legal-list { display: flex; flex-direction: column; gap: 36px; }
  #avro-legal-{{ section.id }} .legal-item { display: flex; flex-direction: column; gap: 12px; }
  #avro-legal-{{ section.id }} .legal-item h2 { font-weight: 800; font-size: clamp(20px,2.4vw,26px); line-height: 1.2; color: var(--ink); margin: 0; }
  #avro-legal-{{ section.id }} .legal-item .rte,
  #avro-legal-{{ section.id }} .legal-item .rte p { font-size: 15px; line-height: 1.65; color: var(--warm-gray); margin: 0 0 12px; }
  #avro-legal-{{ section.id }} .legal-item .rte p:last-child { margin-bottom: 0; }
  #avro-legal-{{ section.id }} .legal-item .rte a { color: var(--ink); text-decoration: underline; }
{% endstyle %}

<div id="avro-legal-{{ section.id }}">
  <section class="legal-hero">
    <h1>{{ section.settings.title | default: 'Returns & Refunds' }}</h1>
    {%- if section.settings.lede != blank -%}<p>{{ section.settings.lede }}</p>{%- endif -%}
  </section>

  <section class="legal-body">
    <div class="legal-wrap">
      {%- if section.settings.updated != blank -%}
        <p class="legal-updated">Last updated: {{ section.settings.updated }}</p>
      {%- endif -%}
      <div class="legal-list">
        {%- for block in section.blocks -%}
          <div class="legal-item" {{ block.shopify_attributes }}>
            <h2>{{ block.settings.heading }}</h2>
            <div class="rte">{{ block.settings.body }}</div>
          </div>
        {%- endfor -%}
      </div>
    </div>
  </section>
</div>

{% schema %}
{
  "name": "AVRO — Returns",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "title", "label": "Page title", "default": "Returns & Refunds" },
    { "type": "textarea", "id": "lede", "label": "Lede", "default": "We want you to feel good about your order. If something is not right, we are here to help." },
    { "type": "text", "id": "updated", "label": "Last updated", "default": "June 2026" }
  ],
  "blocks": [
    {
      "type": "policy_section",
      "name": "Policy section",
      "settings": [
        { "type": "text", "id": "heading", "label": "Heading", "default": "Our guarantee" },
        { "type": "richtext", "id": "body", "label": "Body" }
      ]
    }
  ],
  "presets": [
    {
      "name": "AVRO — Returns",
      "blocks": [
        { "type": "policy_section", "settings": { "heading": "Our guarantee", "body": "<p>If you are not satisfied with your first order, contact us within 30 days of delivery and we will make it right with a replacement or refund.</p>" } },
        { "type": "policy_section", "settings": { "heading": "How to start a return", "body": "<p>Email <a href=\"mailto:hello@avrolife.com\">hello@avrolife.com</a> with your order number and a short note about the issue. Our team will respond with next steps as soon as possible.</p>" } },
        { "type": "policy_section", "settings": { "heading": "Refunds", "body": "<p>Approved refunds are issued to your original payment method. Please allow 5–10 business days for the refund to appear, depending on your bank or card issuer.</p>" } },
        { "type": "policy_section", "settings": { "heading": "Subscriptions", "body": "<p>You can pause, skip, or cancel a subscription at any time from your account before your next billing date. Orders already processed follow the standard return policy above.</p>" } },
        { "type": "policy_section", "settings": { "heading": "Contact us", "body": "<p>Questions about returns can be sent to <a href=\"mailto:hello@avrolife.com\">hello@avrolife.com</a>.</p>" } }
      ]
    }
  ]
}
{% endschema %}
