{%- comment -%}
  ============================================================================
  AVRO — PRODUCT DETAIL (PDP) · ENERGY
  Drop-in export. Gallery images are hardcoded to the live site (one full set
  per flavor). Buy box is wired to Shopify (price, add-to-cart, subscribe).
  • Connect a product: section setting "Connected product" (falls back to the
    template's product when left empty).
  • BOTH flavors always render and are selectable; picking a flavor swaps the
    gallery and selects the matching Shopify variant when one exists.
  NOTE: Deployment Protection on the staging site must be OFF for images to load.
  HOW TO USE
  1. Create sections/product-energy.liquid and paste this file in.
  2. Add the "AVRO Product · Energy" section to a template (or pick a product in the setting).
  ============================================================================
{%- endcomment -%}

{%- assign prod = product -%}
{%- if section.settings.product != blank -%}{%- assign prod = section.settings.product -%}{%- endif -%}

{%- assign accent = '#94C6D4' -%}
{%- assign default_support = 'Steady energy. Ready when it matters.*' -%}

{%- assign flavor_opt_name = section.settings.flavor_option_name | default: 'Flavor' -%}
{%- assign flavor_opt_name_dc = flavor_opt_name | downcase -%}
{%- assign flavor_opt_index = 0 -%}
{%- for opt in prod.options_with_values -%}
  {%- assign onm = opt.name | downcase -%}
  {%- if onm == flavor_opt_name_dc -%}{%- assign flavor_opt_index = forloop.index -%}{%- endif -%}
{%- endfor -%}

{%- assign has_plan = false -%}
{%- if prod.selling_plan_groups.size > 0 -%}{%- assign has_plan = true -%}{%- endif -%}
{%- if section.settings.show_subscribe == false -%}{%- assign has_plan = false -%}{%- endif -%}

{%- case section.settings.button_size -%}
  {%- when 'compact' -%}{%- assign btn_height = 48 -%}
  {%- when 'large' -%}{%- assign btn_height = 64 -%}
  {%- else -%}{%- assign btn_height = 56 -%}
{%- endcase -%}
{%- case section.settings.corner_style -%}
  {%- when 'square' -%}{%- assign btn_radius = 12 -%}
  {%- when 'soft' -%}{%- assign btn_radius = 22 -%}
  {%- else -%}{%- assign btn_radius = 100 -%}
{%- endcase -%}
{%- case section.settings.spacing -%}
  {%- when 'cozy' -%}{%- assign pdp_gap = 16 -%}{%- assign pdp_pad_y = 32 -%}{%- assign pdp_pad_x = 24 -%}
  {%- when 'roomy' -%}{%- assign pdp_gap = 30 -%}{%- assign pdp_pad_y = 56 -%}{%- assign pdp_pad_x = 40 -%}
  {%- else -%}{%- assign pdp_gap = 22 -%}{%- assign pdp_pad_y = 44 -%}{%- assign pdp_pad_x = 32 -%}
{%- endcase -%}

{% style %}
  #avro-pdp-{{ section.id }} {
    --base: #FFFFFF; --base-light: #F2F2F2; --bone: #FFFFFF;
    --charcoal: #151515; --ink: #1C1B14; --warm-gray: #6B6857; --gray: #E2E2E2;
    --avro-blue: {{ accent }};
    --pdp-pad-y: {{ pdp_pad_y }}px;
    --pdp-pad-x: {{ pdp_pad_x }}px;
    --pdp-gap: {{ pdp_gap }}px;
    --btn-height: {{ btn_height }}px;
    --btn-radius: {{ btn_radius }}px;
    font-family: "DM Sans", system-ui, sans-serif; color: var(--ink); display: block;
    background: var(--base-light);
  }
  #avro-pdp-{{ section.id }} * { box-sizing: border-box; }
  #avro-pdp-{{ section.id }} .pdp-wrap { max-width: 1180px; margin: 0 auto; padding: var(--pdp-pad-y) var(--pdp-pad-x); }
  #avro-pdp-{{ section.id }} .pdp-grid { display: grid; grid-template-columns: 1fr; gap: 28px; align-items: start; }
  @media (min-width: 900px) { #avro-pdp-{{ section.id }} .pdp-grid { grid-template-columns: minmax(0,1.05fr) minmax(0,1fr); gap: 48px; } }
  #avro-pdp-{{ section.id }} .gal-main { position: relative; aspect-ratio: 1/1; border-radius: 20px; overflow: hidden; background: var(--gray); }
  #avro-pdp-{{ section.id }} .gal-imgs { position: absolute; inset: 0; }
  #avro-pdp-{{ section.id }} .gal-imgs img { width: 100%; height: 100%; object-fit: cover; display: none; }
  #avro-pdp-{{ section.id }} .gal-imgs img.is-active { display: block; }
  #avro-pdp-{{ section.id }} .gal-rating { position: absolute; left: 14px; top: 14px; display: inline-flex; align-items: center; gap: 6px; background: var(--bone); border-radius: 999px; padding: 6px 12px; font-weight: 700; font-size: 13px; color: var(--ink); z-index: 2; }
  #avro-pdp-{{ section.id }} .gal-rating .stars { color: #E8A23D; letter-spacing: 1px; }
  #avro-pdp-{{ section.id }} .gal-thumbs { display: flex; gap: 10px; margin-top: 12px; flex-wrap: wrap; }
  #avro-pdp-{{ section.id }} .gal-thumb { width: 64px; height: 64px; border-radius: 12px; overflow: hidden; background: var(--gray); border: 2px solid transparent; cursor: pointer; padding: 0; }
  #avro-pdp-{{ section.id }} .gal-thumb.is-active { border-color: var(--charcoal); }
  #avro-pdp-{{ section.id }} .gal-thumb img { width: 100%; height: 100%; object-fit: cover; }
  #avro-pdp-{{ section.id }} .bb { display: flex; flex-direction: column; gap: var(--pdp-gap); }
  #avro-pdp-{{ section.id }} .bb h1 { font-weight: 900; font-size: clamp(26px,3.2vw,40px); line-height: 0.98; margin: 0; color: var(--ink); }
  #avro-pdp-{{ section.id }} .bb .support { font-weight: 600; font-size: clamp(15px,1.3vw,18px); line-height: 1.35; color: var(--ink); margin: 4px 0 0; }
  #avro-pdp-{{ section.id }} .bb .per-box { font-weight: 500; font-size: clamp(13px,1.1vw,15px); line-height: 1.4; color: var(--warm-gray); margin: 2px 0 0; }
  #avro-pdp-{{ section.id }} .bb .lbl { font-weight: 700; font-size: clamp(12px,1vw,13px); color: var(--ink); margin-bottom: 2px; display: block; }
  #avro-pdp-{{ section.id }} .flavors { display: flex; flex-wrap: wrap; gap: 8px; }
  #avro-pdp-{{ section.id }} .flavor { display: inline-flex; align-items: center; gap: 8px; padding: 4px 16px 4px 5px; border-radius: var(--btn-radius); background: var(--gray); border: 2px solid transparent; cursor: pointer; transition: all .15s ease; padding: 10px 18px; }
  #avro-pdp-{{ section.id }} .flavor .nm { font-weight: 700; font-size: clamp(11px,1vw,14px); color: var(--ink); white-space: nowrap; }
  #avro-pdp-{{ section.id }} .flavor.is-active { background: var(--charcoal); border-color: var(--charcoal); }
  #avro-pdp-{{ section.id }} .flavor.is-active .nm { color: var(--bone); }
  #avro-pdp-{{ section.id }} .flavor.is-soldout { opacity: 0.45; }
  #avro-pdp-{{ section.id }} .buy-opt { position: relative; display: flex; align-items: center; gap: 12px; min-height: var(--btn-height); padding: 10px 18px; border-radius: var(--btn-radius); background: var(--gray); border: 2px solid transparent; color: var(--ink); cursor: pointer; }
  #avro-pdp-{{ section.id }} .buy-opt.is-active { background: var(--charcoal); border-color: var(--charcoal); color: var(--bone); }
  #avro-pdp-{{ section.id }} .buy-opt .radio { width: 18px; height: 18px; border-radius: 999px; border: 2px solid var(--warm-gray); display: grid; place-items: center; flex-shrink: 0; }
  #avro-pdp-{{ section.id }} .buy-opt.is-active .radio { border-color: var(--bone); }
  #avro-pdp-{{ section.id }} .buy-opt.is-active .radio::after { content: ''; width: 8px; height: 8px; border-radius: 999px; background: var(--bone); }
  #avro-pdp-{{ section.id }} .buy-opt .ttl { flex: 1; min-width: 0; font-weight: 700; font-size: clamp(12px,1.1vw,15px); line-height: 1.2; }
  #avro-pdp-{{ section.id }} .buy-opt .badge { font-weight: 700; font-size: 9px; letter-spacing: 0.06em; text-transform: uppercase; padding: 2px 6px; border-radius: 999px; background: var(--charcoal); color: var(--bone); white-space: nowrap; margin-left: 6px; }
  #avro-pdp-{{ section.id }} .buy-opt.is-active .badge { background: var(--bone); color: var(--charcoal); }
  #avro-pdp-{{ section.id }} .buy-opt .sub { display: block; font-weight: 500; font-size: clamp(10px,0.9vw,12px); margin-top: 2px; opacity: 0.7; }
  #avro-pdp-{{ section.id }} .buy-opt .pr { font-weight: 800; font-size: clamp(14px,1.3vw,18px); white-space: nowrap; }
  #avro-pdp-{{ section.id }} .benefits { list-style: none; margin: 0; padding: 2px 4px 0; display: flex; flex-direction: column; gap: 6px; }
  #avro-pdp-{{ section.id }} .benefits li { display: flex; align-items: flex-start; gap: 8px; font-weight: 500; font-size: clamp(12px,1vw,14px); line-height: 1.4; color: var(--ink); }
  #avro-pdp-{{ section.id }} .benefits svg { flex-shrink: 0; margin-top: 2px; }
  #avro-pdp-{{ section.id }} .buy-row { display: flex; align-items: stretch; gap: 12px; margin-top: calc(var(--pdp-gap) + 8px); padding-top: var(--pdp-gap); border-top: 1px solid var(--gray); }
  #avro-pdp-{{ section.id }} .stepper { display: inline-flex; align-items: center; justify-content: space-between; flex: 0 0 auto; background: var(--bone); border-radius: var(--btn-radius); border: 2px solid var(--charcoal); height: var(--btn-height); overflow: hidden; }
  #avro-pdp-{{ section.id }} .stepper button { width: 44px; align-self: stretch; border: 0; background: transparent; font-weight: 700; font-size: 18px; line-height: 1; color: var(--charcoal); cursor: pointer; display: grid; place-items: center; }
  #avro-pdp-{{ section.id }} .stepper button:hover { background: rgba(0,0,0,0.05); }
  #avro-pdp-{{ section.id }} .stepper button:disabled { opacity: 0.35; cursor: not-allowed; }
  #avro-pdp-{{ section.id }} .stepper .q { min-width: 26px; text-align: center; font-weight: 800; font-size: 16px; color: var(--charcoal); }
  #avro-pdp-{{ section.id }} .add-btn { flex: 1 1 auto; min-height: var(--btn-height); border-radius: var(--btn-radius); border: 2px solid var(--charcoal); background: var(--charcoal); color: var(--bone); font-family: inherit; font-weight: 700; font-size: 17px; cursor: pointer; transition: background-color .15s ease, color .15s ease; }
  #avro-pdp-{{ section.id }} .add-btn:hover { background: transparent; color: var(--charcoal); }
  #avro-pdp-{{ section.id }} .add-btn:disabled { background: var(--gray); border-color: var(--gray); color: var(--warm-gray); cursor: not-allowed; }
{% endstyle %}

<div id="avro-pdp-{{ section.id }}" data-flavor-index="{{ flavor_opt_index }}">
  <div class="pdp-wrap">
    {%- if prod == blank -%}
      <p style="text-align:center;padding:40px;">Pick a product in the section settings (or add this section to a product template) to preview it.</p>
    {%- else -%}
    <div class="pdp-grid">

      {%- comment -%} ---------- GALLERY (hardcoded per-flavor image sets; JS swaps on flavor change) ---------- {%- endcomment -%}
      <div class="gal">
        <div class="gal-main" id="gal-main-{{ section.id }}">
          <div class="gal-imgs" id="gal-imgs-{{ section.id }}">
            <img data-idx="0" class="is-active" src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/energy-orange-tangerine.png" alt="AVRO Product · Energy Orange Tangerine image 1" loading="eager">
            <img data-idx="1" class="" src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/solo/energy-orange-tangerine.jpg" alt="AVRO Product · Energy Orange Tangerine image 2" loading="lazy">
            <img data-idx="2" class="" src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/sticks/energy-orange-tangerine.png" alt="AVRO Product · Energy Orange Tangerine image 3" loading="lazy">
            <img data-idx="3" class="" src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/lifestyle/glass-energy-orange-tangerine.jpg" alt="AVRO Product · Energy Orange Tangerine image 4" loading="lazy">
            <img data-idx="4" class="" src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/supplement-facts/orange-tangerine.png" alt="AVRO Product · Energy Orange Tangerine image 5" loading="lazy">
          </div>
          {%- if section.settings.show_rating -%}
            <span class="gal-rating"><span class="stars">★★★★★</span> {{ section.settings.rating_value | default: '4.8' }}</span>
          {%- endif -%}
        </div>
        <div class="gal-thumbs" id="gal-thumbs-{{ section.id }}">
            <button type="button" class="gal-thumb is-active" data-target="0" aria-label="View image 1">
              <img src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/energy-orange-tangerine.png" alt="">
            </button>
            <button type="button" class="gal-thumb " data-target="1" aria-label="View image 2">
              <img src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/solo/energy-orange-tangerine.jpg" alt="">
            </button>
            <button type="button" class="gal-thumb " data-target="2" aria-label="View image 3">
              <img src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/sticks/energy-orange-tangerine.png" alt="">
            </button>
            <button type="button" class="gal-thumb " data-target="3" aria-label="View image 4">
              <img src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/lifestyle/glass-energy-orange-tangerine.jpg" alt="">
            </button>
            <button type="button" class="gal-thumb " data-target="4" aria-label="View image 5">
              <img src="https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/supplement-facts/orange-tangerine.png" alt="">
            </button>
        </div>
      </div>

      {%- comment -%} ---------- BUY BOX (wired to Shopify) ---------- {%- endcomment -%}
      <div class="bb">
        <header>
          <h1>{{ prod.title }}</h1>
          {%- assign support_text = section.settings.support_line -%}
          {%- if support_text == blank -%}{%- assign support_text = default_support -%}{%- endif -%}
          {%- if prod.metafields.custom.support_line != blank -%}{%- assign support_text = prod.metafields.custom.support_line -%}{%- endif -%}
          {%- if support_text != blank -%}<p class="support">{{ support_text }}</p>{%- endif -%}
          {%- if section.settings.per_box_line != blank -%}<p class="per-box">{{ section.settings.per_box_line }}</p>{%- endif -%}
        </header>

        <form method="post" action="{{ routes.cart_add_url }}" id="pdp-form-{{ section.id }}" accept-charset="UTF-8" enctype="multipart/form-data">
          <input type="hidden" name="form_type" value="product">
          <input type="hidden" name="utf8" value="✓">
          <input type="hidden" name="id" id="pdp-vid-{{ section.id }}" value="{{ prod.selected_or_first_available_variant.id }}">
          <input type="hidden" name="selling_plan" id="pdp-plan-{{ section.id }}" value="" {% unless has_plan %}disabled{% endunless %}>

          {%- comment -%} Flavor selector — BOTH flavors always render (independent of Shopify options) {%- endcomment -%}
          <div class="flavor-block" style="display:flex;flex-direction:column;gap:6px;">
            <span class="lbl">{{ flavor_opt_name }}</span>
            <div class="flavors" role="radiogroup" aria-label="{{ flavor_opt_name }}">
                <button type="button" class="flavor is-active" data-flavor-key="orange-tangerine" data-flavor-label="Orange Tangerine" role="radio" aria-checked="true">
                  <span class="nm">Orange Tangerine</span>
                </button>
                <button type="button" class="flavor" data-flavor-key="fuji-apple" data-flavor-label="Fuji Apple" role="radio" aria-checked="false">
                  <span class="nm">Fuji Apple</span>
                </button>
            </div>
          </div>

          {%- if has_plan -%}
            {%- assign first_group = prod.selling_plan_groups | first -%}
            {%- assign first_plan = first_group.selling_plans | first -%}
            <div class="purchase" style="display:flex;flex-direction:column;gap:10px;">
              <button type="button" class="buy-opt is-active" data-buy="subscribe" data-plan="{{ first_plan.id }}">
                <span class="radio"></span>
                <span class="ttl">{{ section.settings.subscribe_label | default: 'Subscribe & save' }}
                  {%- if section.settings.subscribe_badge != blank %}<span class="badge">{{ section.settings.subscribe_badge }}</span>{% endif -%}
                  <span class="sub">{{ first_plan.name }}</span>
                </span>
                <span class="pr" data-price-subscribe></span>
              </button>
              <button type="button" class="buy-opt" data-buy="onetime">
                <span class="radio"></span>
                <span class="ttl">{{ section.settings.onetime_label | default: 'One-time purchase' }}
                  <span class="sub">{{ section.settings.onetime_sub | default: 'No commitment.' }}</span>
                </span>
                <span class="pr" data-price-onetime></span>
              </button>
              {%- assign benefits = section.blocks | where: 'type', 'benefit' -%}
              {%- if benefits.size > 0 -%}
                <ul class="benefits" data-subscribe-benefits>
                  {%- for b in benefits -%}
                    <li {{ b.shopify_attributes }}>
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M5 12.5l4.5 4.5L19 7" stroke="var(--avro-blue)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                      {{ b.settings.text }}
                    </li>
                  {%- endfor -%}
                </ul>
              {%- endif -%}
            </div>
          {%- else -%}
            <div class="price-only" style="font-weight:800;font-size:clamp(20px,2vw,28px);" data-price-onetime></div>
          {%- endif -%}

          <div class="buy-row">
            <div class="stepper" role="group" aria-label="Quantity">
              <button type="button" data-step="-1" aria-label="Decrease quantity" disabled>&minus;</button>
              <span class="q" id="pdp-qty-{{ section.id }}">1</span>
              <button type="button" data-step="1" aria-label="Increase quantity">+</button>
            </div>
            <input type="hidden" name="quantity" id="pdp-qtyinput-{{ section.id }}" value="1">
            <button type="submit" class="add-btn" id="pdp-add-{{ section.id }}">{{ section.settings.add_label | default: 'Add to Cart' }}</button>
          </div>
        </form>
      </div>

    </div>
    {%- endif -%}
  </div>
</div>

{%- if prod != blank -%}
<script>
  (function () {
    var root = document.getElementById("avro-pdp-{{ section.id }}");
    if (!root) return;
    var variants = {{ prod.variants | json }};
    var flavorIndex = parseInt(root.dataset.flavorIndex || "0", 10);
    var hasPlan = {{ has_plan | json }};

    var GALLERIES = {"orange-tangerine":["https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/energy-orange-tangerine.png","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/solo/energy-orange-tangerine.jpg","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/sticks/energy-orange-tangerine.png","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/lifestyle/glass-energy-orange-tangerine.jpg","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/supplement-facts/orange-tangerine.png"],"fuji-apple":["https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/studio/energy-fuji-apple.png","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/tubes/solo/energy-fuji-apple.jpg","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/sticks/energy-fuji-apple.png","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/lifestyle/glass-energy-fuji-apple.jpg","https://v0-build-request-git-v0-joe-6434-a0cfbf5f-joe-6434s-projects.vercel.app/images/supplement-facts/fuji-apple.png"]};
    var FLAVOR_MATCH = [{"key":"orange-tangerine","label":"Orange Tangerine","tokens":["orange","tangerine"]},{"key":"fuji-apple","label":"Fuji Apple","tokens":["fuji","apple"]}];

    var vidInput = document.getElementById("pdp-vid-{{ section.id }}");
    var planInput = document.getElementById("pdp-plan-{{ section.id }}");
    var qtyEl = document.getElementById("pdp-qty-{{ section.id }}");
    var qtyInput = document.getElementById("pdp-qtyinput-{{ section.id }}");
    var addBtn = document.getElementById("pdp-add-{{ section.id }}");
    var galImgs = document.getElementById("gal-imgs-{{ section.id }}");
    var galThumbs = document.getElementById("gal-thumbs-{{ section.id }}");

    function tokensForKey(key) {
      for (var i = 0; i < FLAVOR_MATCH.length; i++) { if (FLAVOR_MATCH[i].key === key) return FLAVOR_MATCH[i].tokens; }
      return [];
    }
    // Find the Shopify variant whose flavor option matches the given flavor key (by token). May be null.
    function variantForKey(key) {
      var toks = tokensForKey(key);
      for (var i = 0; i < variants.length; i++) {
        var v = variants[i];
        var val = flavorIndex > 0 ? (v["option" + flavorIndex] || "") : (v.title || "");
        val = val.toString().toLowerCase();
        for (var j = 0; j < toks.length; j++) { if (val.indexOf(toks[j]) > -1) return v; }
      }
      return null;
    }

    var defaultKey = FLAVOR_MATCH[0].key;
    var state = { key: defaultKey, variant: variantForKey(defaultKey) || variants[0], buy: hasPlan ? "subscribe" : "onetime", qty: 1 };

    function money(cents) { return "$" + (cents / 100).toFixed(2); }
    function planAllocation(v) {
      if (!v || !v.selling_plan_allocations || !v.selling_plan_allocations.length) return null;
      return v.selling_plan_allocations[0];
    }
    function renderGallery(key) {
      var urls = GALLERIES[key] || GALLERIES[defaultKey];
      if (!urls || !galImgs || !galThumbs) return;
      galImgs.innerHTML = urls.map(function (src, i) {
        return '<img data-idx="' + i + '" class="' + (i === 0 ? 'is-active' : '') + '" src="' + src + '" alt="" loading="' + (i === 0 ? 'eager' : 'lazy') + '">';
      }).join("");
      galThumbs.innerHTML = urls.map(function (src, i) {
        return '<button type="button" class="gal-thumb ' + (i === 0 ? 'is-active' : '') + '" data-target="' + i + '" aria-label="View image ' + (i + 1) + '"><img src="' + src + '" alt=""></button>';
      }).join("");
      bindThumbs();
    }
    function bindThumbs() {
      root.querySelectorAll(".gal-thumb").forEach(function (btn) {
        btn.addEventListener("click", function () {
          var idx = btn.dataset.target;
          galImgs.querySelectorAll("img").forEach(function (img) { img.classList.toggle("is-active", img.dataset.idx == idx); });
          root.querySelectorAll(".gal-thumb").forEach(function (t) { t.classList.toggle("is-active", t === btn); });
        });
      });
    }

    function render() {
      var v = state.variant;
      root.querySelectorAll("[data-price-onetime]").forEach(function (el) { el.textContent = v ? money(v.price * state.qty) : ""; });
      var alloc = planAllocation(v);
      root.querySelectorAll("[data-price-subscribe]").forEach(function (el) { el.textContent = alloc ? money(alloc.price * state.qty) : (v ? money(v.price * state.qty) : ""); });

      vidInput.value = v ? v.id : "";
      if (hasPlan) {
        if (state.buy === "subscribe" && alloc) { planInput.disabled = false; planInput.value = alloc.selling_plan.id; }
        else { planInput.value = ""; planInput.disabled = true; }
      }
      // Add-to-cart is enabled as long as we have a matching variant that's available.
      var available = v && v.available;
      addBtn.disabled = !v || !available;
      addBtn.textContent = (!v) ? "Unavailable" : (available ? "{{ section.settings.add_label | default: 'Add to Cart' | escape }}" : "Sold Out");
    }

    // Flavor selection (both buttons always present)
    root.querySelectorAll(".flavor").forEach(function (btn) {
      btn.addEventListener("click", function () {
        var key = btn.dataset.flavorKey;
        root.querySelectorAll(".flavor").forEach(function (b) { b.classList.remove("is-active"); b.setAttribute("aria-checked", "false"); });
        btn.classList.add("is-active"); btn.setAttribute("aria-checked", "true");
        state.key = key;
        var match = variantForKey(key);
        state.variant = match || state.variant;
        renderGallery(key);
        render();
      });
    });

    renderGallery(state.key);
    render();
  })();
</script>
{%- endif -%}

{% schema %}
{
  "name": "AVRO Product · Energy",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Product"
    },
    {
      "type": "product",
      "id": "product",
      "label": "Connected product",
      "info": "Leave empty to use the product this template is assigned to."
    },
    {
      "type": "header",
      "content": "Copy"
    },
    {
      "type": "textarea",
      "id": "support_line",
      "label": "Support line",
      "info": "The product metafield custom.support_line overrides this.",
      "default": "Steady energy. Ready when it matters.*"
    },
    {
      "type": "text",
      "id": "per_box_line",
      "label": "Per-box line",
      "default": "10 stick packets per box"
    },
    {
      "type": "header",
      "content": "Flavor"
    },
    {
      "type": "text",
      "id": "flavor_option_name",
      "label": "Flavor label",
      "info": "Heading shown above the flavor buttons, and the Shopify option name used to match variants (case-insensitive). Both flavors always show: Orange Tangerine / Fuji Apple.",
      "default": "Flavor"
    },
    {
      "type": "header",
      "content": "Rating badge"
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "Show rating badge",
      "default": true
    },
    {
      "type": "text",
      "id": "rating_value",
      "label": "Rating value",
      "default": "4.8"
    },
    {
      "type": "header",
      "content": "Purchase options"
    },
    {
      "type": "checkbox",
      "id": "show_subscribe",
      "label": "Show subscribe option (when product has a selling plan)",
      "default": true
    },
    {
      "type": "text",
      "id": "subscribe_label",
      "label": "Subscribe label",
      "default": "Subscribe & save 15% every order of 2 or more tubes"
    },
    {
      "type": "text",
      "id": "subscribe_badge",
      "label": "Subscribe badge",
      "default": "Most popular"
    },
    {
      "type": "text",
      "id": "onetime_label",
      "label": "One-time label",
      "default": "One-time purchase"
    },
    {
      "type": "text",
      "id": "onetime_sub",
      "label": "One-time subtitle",
      "default": "No commitment. Free shipping over $50."
    },
    {
      "type": "text",
      "id": "add_label",
      "label": "Add-to-cart label",
      "default": "Add to Cart"
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Subscribe benefit",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Benefit",
          "default": "Skip, change or cancel anytime."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "AVRO Product · Energy",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "text": "Select your own delivery schedule."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Save 15% on every order."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Free shipping on all orders of 2 or more tubes."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Skip, change or cancel anytime."
          }
        }
      ]
    }
  ]
}
{% endschema %}
